import { ShoeColorDirective } from './shoe-color.directive';

describe('ShoeColorDirective', () => {
  it('should create an instance', () => {
    const directive = new ShoeColorDirective();
    expect(directive).toBeTruthy();
  });
});
